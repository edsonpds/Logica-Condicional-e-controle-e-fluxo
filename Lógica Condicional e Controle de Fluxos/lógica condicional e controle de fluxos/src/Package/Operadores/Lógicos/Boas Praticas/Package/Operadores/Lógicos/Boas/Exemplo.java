package Package.Operadores.Lógicos.Boas Praticas;

public class Exemplo {
    public static void main(String[] args) {
        
        /*
         - Crie variáveis para guardar resultados intermediários
         (salarioMensal < mediaSalarial) && (quantidadeDependentes >=
         mediaDependentes)
         pode ser
         (salarioBaixo) && (muitosDependentes)

         boolean recebeAuxilio = (salarioBaixo) && 
         (muitosDependentes);
         */
    }
    
}
